package com.wedding.planner.domain;

/** Which project-scoped entity a log row is about. */
public enum ActivityEntityType {
    PROJECT,
    TASK,
    VENDOR,
    VENDOR_PAYMENT,
    EXPENSE,
    GUEST,
    INVITATION,
    TIMELINE_EVENT,
    ATTACHMENT,
    ENTOURAGE_MEMBER
}
